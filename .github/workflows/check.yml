name: Check compile

on:
  push:
  pull_request:
  workflow_dispatch:


jobs:
  check:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Check code
        run: cargo check
